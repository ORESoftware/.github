{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.oresoftware.com/governance/organization-source-of-truth-pointer.v1.schema.json",
  "title": "ORE organization source-of-truth mirror pointer",
  "description": "Independent JSON Schema authority for non-canonical repository pointers to an owner-level organization manifest.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schemaVersion",
    "organization",
    "authorityRepository",
    "canonicalPath",
    "canonicalRevision",
    "observedAt",
    "status",
    "mirrorRepository",
    "mirrorMode"
  ],
  "properties": {
    "schemaVersion": {
      "const": "ores.organization-source-of-truth-pointer.v1"
    },
    "organization": {
      "type": "string",
      "minLength": 1,
      "pattern": "^[A-Za-z0-9](?:[A-Za-z0-9-]{0,37}[A-Za-z0-9])?$"
    },
    "authorityRepository": {
      "$ref": "#/$defs/repositoryName"
    },
    "canonicalPath": {
      "const": "governance/source-of-truth.toml"
    },
    "canonicalRevision": {
      "type": "integer",
      "minimum": 1
    },
    "observedAt": {
      "type": "string",
      "format": "date-time"
    },
    "status": {
      "type": "string",
      "enum": ["current", "stale", "blocked", "unverified"]
    },
    "canonicalDigest": {
      "type": "string",
      "pattern": "^sha256:[a-f0-9]{64}$"
    },
    "mirrorRepository": {
      "$ref": "#/$defs/repositoryName"
    },
    "mirrorMode": {
      "const": "pointer"
    },
    "note": {
      "type": "string",
      "minLength": 1
    }
  },
  "$defs": {
    "repositoryName": {
      "type": "string",
      "pattern": "^[A-Za-z0-9_.-]+/[A-Za-z0-9_.-]+$"
    }
  }
}
